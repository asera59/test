# 今日清单（2026-09-19，星期六，启动段第 2 天）

> 每做完一项打 ✓。**今日已达标**（判据见文末），下面是实际完成情况与剩余可选项。

## 一、看（实际进度：看到 P13）

- [ ] P3　1.1.2 计算机的思维方式：重复是计算机最擅长的 —— 强烈建议，还没看
- [x] P6　1.2.3 编程软件：我们推荐 Dev C++
- [x] P7　1.3.1 第一个 C 程序
- [x] P8　1.3.2 详解第一个程序：程序框架、printf、出错怎么办
- [x] P9　1.3.3 做点计算：让程序输出算术结果
- [x] P12　2.1.2 变量定义
- [x] P13　2.1.3 变量赋值与初始化 ← 停在这里
- [ ] P14　2.1.4 变量输入 scanf —— 还没看（代码里已试过 scanf 那段）

## 二、敲（实际进度：①②③ 已跑通）

- [x] 练习① 顺序执行 → `code/2026-09-19_printf.c`
- [x] 练习② 变量是盒子 → `code/2026-09-19_var_loop_for.c`
- [x] 练习③ for 循环（打印 1 到 10）→ 同上文件
- [ ] 练习④ while(1) + break
- [ ] 练习⑤ 数组（`unsigned char led[4] = {0xFE,0xFD,0xFB,0xF7};`）
- [x] 额外：`sizeof` 各种类型占几个字节 → `code/2026-09-18_sizeof.c`（9/18 写的）

**今天踩过的两个真实报错**（都已解决，值得记一辈子）：
1. 中文输入法导致的全角标点 → `stray '\243'`，解决办法：删掉重打，输入法切英文
2. `for (int i = 1; ...)` → C99 报错，解决办法：`int i;` 提到 for 前面

## 三、Keil 那 7 行（今天不要求懂）

- [ ] Keil μVision 选 **Use Simulator** 编译通过
- [ ] Watch 窗口里看到 `i` 在涨

## 四、Git 首次提交（今天唯一"过期作废"的事）

**详细步骤见 `docs\Git操作步骤.md`**。当前状态：`git init` 与 `git add .` 已完成（21 个文件已暂存），只差下面两步。

- [x] `git config --global user.name`（已完成）
- [ ] `git config --global user.email "你的 GitHub 邮箱"` ← **卡在这里**
- [x] `git init`（已在 `D:\code` 完成）
- [x] `git add .`（已暂存 21 个文件，无垃圾混入）
- [ ] `git commit -m "启动段 D2：C 语言前三个练习 + 原理速查笔记"`
- [ ] `git log --oneline` 验证日期是今天
- [ ] GitHub 建私有仓库 → `remote add` → `branch -M main` → `push -u origin main`

## 今日通过判据 —— 已达成 ✓

**有一个程序在 Dev-C++ 里跑起来、看到输出变了** → 练习①②③ 全部跑通，判据达成。

剩下的都是加分项。手绘链路图挪到明天（9/20 补课日，1.5 小时，任务量下调）。

## 仓库结构（`D:\code` 为 GitHub 仓库根）

```
D:\code\
├── .gitignore                   挡住 VS 缓存与 .exe
├── today.md                     今日清单（本文件）
├── docs\原理速查.md             原理速查，15 节
└── 2026\                        练习代码，按日期分文件夹
    ├── 9.18\9.18.c              sizeof 练习
    ├── 9.19\9.19.c              练习①
    ├── test9.19\test hello.c    练习②③
    └── test.9.17\hello.c        早期尝试
```

**以后练习继续按 `2026\日期\` 建文件夹**，和现在习惯一致。

## 工具分工（别再换工具）

- **Dev-C++**（`D:\Dev-Cpp`，F11 编译运行）—— 学 C 语法
- **Keil μVision 5**（`D:\Keil_v5`）—— 写单片机代码、编译、仿真
- **VS Code**（`D:\Microsoft VS Code`）—— 写笔记/管理 Git
- **STC-ISP** —— 10/8 之后用

## 卡住先来这里

- 报错不知道什么意思 → 速查第 8 节「常见报错对照」
- 不认识的英文单词 → 速查第 14 节「英文速查表」
- 编译不通过、漏分号、中文标点 → 全删重打，输入法切到英文
- 看视频看不懂 → 关掉视频，敲代码比看完一遍记得牢
